<div id="ga-consent-banner" class="ga-consent-banner" role="dialog" aria-label="Cookie consent" aria-live="polite" hidden>
  <img
    class="ga-consent-banner__image"
    src="{{ '/assets/img/cookie-cow.png' | relative_url }}"
    alt="Cookie cow"
    loading="lazy"
  >
  <p class="ga-consent-banner__text">We use analytics cookies to understand site usage.</p>
  <div class="ga-consent-banner__actions">
    <button type="button" class="ga-consent-banner__btn ga-consent-banner__btn--ghost" data-ga-consent-action="deny">Decline</button>
    <button type="button" class="ga-consent-banner__btn ga-consent-banner__btn--primary" data-ga-consent-action="accept">
      Accept
    </button>
  </div>
</div>
<style>
  .ga-consent-banner {
    position: fixed;
    right: 1rem;
    bottom: 1rem;
    z-index: 1080;
    display: flex;
    gap: 0.75rem;
    align-items: center;
    max-width: 28rem;
    padding: 0.75rem 0.9rem;
    border: 1px solid rgba(0, 0, 0, 0.12);
    border-radius: 0.6rem;
    background: #ffffff;
    color: #212529;
    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.12);
  }

  .ga-consent-banner[hidden] {
    display: none;
  }

  .ga-consent-banner__text {
    margin: 0;
    font-size: 0.9rem;
    line-height: 1.3;
  }

  .ga-consent-banner__image {
    width: 3rem;
    height: 3rem;
    border-radius: 0.45rem;
    object-fit: cover;
    flex-shrink: 0;
  }

  .ga-consent-banner__actions {
    display: flex;
    gap: 0.45rem;
    margin-left: auto;
  }

  .ga-consent-banner__btn {
    border: 1px solid transparent;
    border-radius: 0.45rem;
    padding: 0.35rem 0.65rem;
    font-size: 0.85rem;
    line-height: 1.2;
    cursor: pointer;
    white-space: nowrap;
    transition: background-color 120ms ease, border-color 120ms ease;
  }

  .ga-consent-banner__btn--ghost {
    border-color: rgba(0, 0, 0, 0.2);
    background: #ffffff;
    color: #212529;
  }

  .ga-consent-banner__btn--ghost:hover {
    background: #f8f9fa;
  }

  .ga-consent-banner__btn--primary {
    border-color: #212529;
    background: #212529;
    color: #ffffff;
  }

  .ga-consent-banner__btn--primary:hover {
    background: #343a40;
    border-color: #343a40;
  }

  @media (max-width: 640px) {
    .ga-consent-banner {
      left: 0.75rem;
      right: 0.75rem;
      bottom: 0.75rem;
      max-width: none;
      flex-direction: column;
      align-items: stretch;
    }

    .ga-consent-banner__actions {
      margin-left: 0;
      justify-content: flex-end;
    }

    .ga-consent-banner__image {
      width: 4rem;
      height: 4rem;
      align-self: center;
    }
  }
</style>
